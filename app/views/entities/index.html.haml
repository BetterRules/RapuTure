.card-columns
  - @entities.each do |entity|
    .card{:style => "width: 18rem;"}
      .card-body
        %h5.card-title
          = link_to entity.description, entity
        %p.card-text
          = render 'badges', entity: entity
          = render_markdown(entity.documentation) if entity.documentation.present?
