.card
  .card-body
    %ul.list-unstyled
      - values.each do |key, value|
        %li
          %b= "#{key.to_s.capitalize}: "
          - value.each do |v|
            = number_to_currency(v.last)
          %hr
