.container
  %h2.display-3= @parameter.description
  .row
    .col-md-12
      .display-3= link_to @parameter.href.match(/https?:\/\/[\S]+/), "#{@parameter.href.match(/https?:\/\/[\S]+/)}"
  .row
    .col-md-12
      %h2 Brackets
      = render 'card', brackets: @brackets, values: @values
