- values.each do |val|
  .card-body
    %ul.list-unstyled
      - val.each do |key, val1|
        .card
          .card-header
            %li
              %h6= key.to_s.capitalize

          .card-body
            %ul
              - unless val1&.nil?
                - val1.each do |key2, val2|
                  %p
                    %b= "#{key2}:"
                    - val2.each do |val3|
                      = number_to_currency(val3.last)
